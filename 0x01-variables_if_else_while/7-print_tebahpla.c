#include <stdio.h>
/**
 *main - Entry point
 *Description -prints lowercase
 *Return: return 0(Success)
*/
int main(void)
{
char c;
c = 'r';
while
(c >= 'a') {
pitchar(c);
c--;
}
putchar('\n');
return (0);
}
